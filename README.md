# KhEIa – Unlock Your Future

Mobile-first exam prep app for EN/Bac with offline-first data, AI generation,
and gamified study flows.

## Quick start

1. Copy `.env.example` to `.env` and fill required values.
2. Install dependencies in root and `node-backend`.
3. Run Expo dev server.

## Structure

- `app/` Expo Router screens and layouts
- `src/` Shared app code (components, services, database, hooks)
- `supabase/` Migrations and Edge Functions
- `node-backend/` Railway Node.js AI orchestration service
