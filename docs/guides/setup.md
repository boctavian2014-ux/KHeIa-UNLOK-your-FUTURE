# Setup

1. Install Node.js 22 and npm.
2. Install Expo CLI: `npm install -g expo-cli`.
3. Copy `.env.example` to `.env` and set values.
4. Run `npm install` in root and `node-backend`.
5. Start Expo: `npm run start`.

## Baza de date Supabase

Pentru configurarea bazei de date cloud, vezi [supabase-setup.md](./supabase-setup.md).
